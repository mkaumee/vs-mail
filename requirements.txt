pymupdf>=1.24
openpyxl>=3.1
python-docx>=1.1
httpx>=0.27
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.8
google-api-python-client>=2.140
google-auth-oauthlib>=1.2
google-auth-httplib2>=0.2

# Local embeddings for the knowledge base. No API and no second key:
# DeepSeek has no embeddings endpoint, and the model is committed under
# models/ so a deploy needs nothing from HuggingFace.
onnxruntime>=1.20
tokenizers>=0.20
numpy>=1.26
